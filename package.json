{
  "name": "taskflow",
  "version": "1.0.0",
  "description": "Project & task manager with role-based access control",
  "scripts": {
    "build": "cd server && npm install && npx prisma generate && cd ../frontend && npm install && npm run build",
    "start": "cd server && npx prisma db push && node src/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
