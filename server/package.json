{
  "name": "taskflow",
  "version": "1.0.0",
  "description": "Project management app with role-based access control",
  "main": "src/server.js",
  "scripts": {
    "start": "npx prisma db push && node src/server.js",
    "dev": "nodemon src/server.js",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "npx prisma generate && cd ../frontend && npm install && npm run build",
    "postinstall": "npx prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "express-validator": "^7.2.0",
    "helmet": "^8.0.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.7",
    "prisma": "^5.22.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
